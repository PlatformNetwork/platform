[package]
name = "check-duplicate-instance-ids"
version = "0.1.0"
edition = "2021"
description = "Checks for duplicate instance_ids across all workspace.yaml files in hf-tasks/tasks"

[dependencies]
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3.12"
